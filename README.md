# set up the nod eapplication

# npm i eslint @eslint/js prettier eslint-config-prettier eslint-plugin-prettier -D => for setting up EsLint Prettier

# add 3 file; eslint.config.js, .prettierrc, .prettierignore
